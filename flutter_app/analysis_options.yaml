# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  # Extra rules on top of flutter_lints — the defaults leave a lot of
  # well-known foot-guns enabled (unawaited futures, dynamic calls,
  # print statements slipping into release). Keep this list aligned with
  # code-review finding §2.2.
  rules:
    - avoid_print
    - prefer_const_constructors
    - prefer_const_constructors_in_immutables
    - prefer_const_declarations
    - prefer_const_literals_to_create_immutables
    - unawaited_futures
    - avoid_dynamic_calls
    - use_build_context_synchronously
    - require_trailing_commas
