# config.js holds the project's anon key. It's public by design, but we
# still keep it out of git so the example file is the single source of
# truth in the repo and each deploy can have its own values.
config.js
.vercel/
node_modules/

# Generated from index.html at deploy time by deploy-surge.sh (Surge's
# SPA-fallback file for the /s/<token> route). index.html is the source
# of truth — never edit 200.html by hand.
200.html
